fullnameOverride: cnpg
replicaCount: 2
monitoring:
  podMonitorEnabled: true
resources:
  requests: { cpu: 50m, memory: 128Mi }
  limits:   { memory: 256Mi }
podDisruptionBudget:
  enabled: true
  minAvailable: 1
